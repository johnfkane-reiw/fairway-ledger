{
  "name": "fairway-ledger",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev dist",
    "test": "node test/whs.test.mjs",
    "db:local": "wrangler d1 execute fairway-ledger --local --file=./migrations/0001_init.sql",
    "db:remote": "wrangler d1 execute fairway-ledger --remote --file=./migrations/0001_init.sql"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11",
    "wrangler": "^3.90.0"
  }
}
